[project]
name = "legal_service"
version = "0.1.0"
description = "Legal module service for contract lifecycle, aligned with Core"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
  "psycopg[binary]>=3.2.0",
  "pydantic>=2.9.0",
  "python-dotenv>=1.0.1",
]
