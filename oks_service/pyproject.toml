[project]
name = "oks_service"
version = "0.1.0"
description = "OKS Service — капитальное строительство (объекты, этапы, документы)"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
  "psycopg[binary]>=3.2.0",
  "pydantic>=2.9.0",
  "python-dotenv>=1.0.1",
]
